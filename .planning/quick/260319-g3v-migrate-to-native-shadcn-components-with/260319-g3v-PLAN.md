---
phase: quick
plan: 260319-g3v
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/Dashboard.jsx
autonomous: true
requirements: [QUICK-001]

must_haves:
  truths:
    - "Dashboard renders with a white/light background, no dark theme artifacts"
    - "All text is readable on light backgrounds (dark text on white)"
    - "Charts have visible gridlines and labels on light background"
    - "npm run build passes with zero errors"
  artifacts:
    - path: "app/globals.css"
      provides: "Clean oklch light theme, no .dark block, no conflicting hsl() lines"
    - path: "components/Dashboard.jsx"
      provides: "All Tailwind classes use semantic tokens or light-friendly equivalents"
  key_links:
    - from: "app/globals.css"
      to: "components/Dashboard.jsx"
      via: "CSS custom properties consumed by Tailwind semantic classes"
      pattern: "bg-(background|card|muted|popover)|text-(foreground|muted-foreground|popover-foreground)|border-border"
---

<objective>
Migrate the dashboard from hardcoded dark-theme Tailwind classes to shadcn semantic tokens with a default light theme.

Purpose: The shadcn components were installed via CLI but globals.css has conflicting legacy lines and Dashboard.jsx still uses dark-specific Tailwind classes (bg-slate-800, text-slate-200, etc). This plan cleans up CSS and replaces all dark-theme classes so the dashboard renders correctly on the shadcn default light theme.

Output: A fully light-themed dashboard using shadcn semantic CSS variables throughout.
</objective>

<execution_context>
@/Users/andrewmaher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewmaher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/globals.css
@components/Dashboard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up globals.css for light-only theme</name>
  <files>app/globals.css</files>
  <action>
Edit app/globals.css to remove conflicting legacy styles and the dark theme block:

1. In the `*` rule (line 44-47), remove the line `border-color: hsl(var(--border));` — the `@apply border-border` on the next line already handles this via oklch. The hsl() call conflicts with oklch values.

2. In the `body` rule (lines 49-56), remove `background-color: hsl(var(--background));` and `color: hsl(var(--foreground));` — the `@apply bg-background text-foreground` already handles this. Same hsl/oklch conflict.

3. Delete the entire `.dark { ... }` block (lines 57-89) since the user wants light-only.

4. Keep everything else untouched: the @import lines, :root vars, .theme block, html rule.
  </action>
  <verify>
    <automated>cd /Users/andrewmaher/invitro-dashboard-1 && grep -c "hsl(var" app/globals.css && echo "FAIL: hsl(var still present" || echo "PASS: no hsl(var references"; grep -c "\.dark" app/globals.css && echo "FAIL: .dark block still present" || echo "PASS: no .dark block"</automated>
  </verify>
  <done>globals.css has no hsl(var(...)) lines, no .dark block. Only oklch-based :root vars and @apply directives remain.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dark-theme classes in Dashboard.jsx with semantic light-theme equivalents</name>
  <files>components/Dashboard.jsx</files>
  <action>
Edit components/Dashboard.jsx to replace all hardcoded dark-theme Tailwind classes with semantic tokens. Specific replacements:

**CHART_STYLE constants (lines 16-22):**
Replace the entire object with light-friendly values:
```
const CHART_STYLE = {
  positive: "#16a34a",   // green-600 (visible on white)
  negative: "#dc2626",   // red-600
  muted: "#64748b",      // slate-500 (readable on white)
  border: "#e2e8f0",     // slate-200 (light grid lines)
  totalLine: "#0f172a",  // slate-900 (dark line on white bg)
};
```

**CustomTooltip (line 28):**
- `border-slate-600 bg-slate-800` -> `border-border bg-popover`
- `text-slate-200` -> `text-popover-foreground`

**KPICard trend colors (line 47):**
- `text-emerald-400` -> `text-emerald-600`
- `text-red-400` -> `text-red-600`

**InsightCard bgMap (line 59):**
Replace dark bg values with light equivalents:
```
const bgMap = {
  positive: "bg-emerald-50 border-emerald-300",
  warning: "bg-amber-50 border-amber-300",
  danger: "bg-red-50 border-red-300",
  info: "bg-blue-50 border-blue-300",
};
```

**Header (line 247):**
- `bg-gradient-to-r from-slate-800 to-slate-900` -> `bg-card`
- Keep `border-b border-border/50`

**TabsList (line 272):**
- `bg-slate-800/80` -> remove this class entirely (let shadcn default apply)

**Company table revenue growth (line 358):**
- `text-emerald-400` -> `text-emerald-600`
- `text-red-400` -> `text-red-600`

**EBITDA column (line 366):**
- `text-emerald-400` -> `text-emerald-600`
- `text-red-400` -> `text-red-600`

**TableFooter row (line 376):**
- `bg-slate-800/80 hover:bg-slate-800/80` -> `bg-muted hover:bg-muted`
- `text-emerald-400` on EBITDA total -> `text-emerald-600`

**YoY bars (line 449):**
- `fill="#475569"` -> `fill="#94a3b8"` (slate-400, lighter for prior year on white)

**Footer deploy link (line 607):**
- `text-blue-400 hover:text-blue-300` -> `text-blue-600 hover:text-blue-500`

**Footer deploy message (line 612):**
- `text-emerald-400` -> `text-emerald-600`

Keep ALL layouts, component structure, data logic, and non-color classes unchanged.
  </action>
  <verify>
    <automated>cd /Users/andrewmaher/invitro-dashboard-1 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard.jsx has zero instances of slate-800, slate-900, slate-600, slate-200 hardcoded classes. All color classes use semantic tokens (bg-card, bg-muted, text-foreground, border-border) or light-appropriate values (emerald-600, red-600). Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -E "slate-[89]00|slate-600|slate-200" components/Dashboard.jsx` returns no matches (no dark hardcoded classes)
3. `grep -c "hsl(var" app/globals.css` returns 0
4. `grep -c "\.dark" app/globals.css` returns 0
</verification>

<success_criteria>
- Build passes cleanly
- No dark-theme hardcoded classes remain in Dashboard.jsx
- globals.css is clean: oklch light theme only, no hsl/oklch conflicts, no .dark block
- All chart colors are visible on white backgrounds
</success_criteria>

<output>
After completion, create `.planning/quick/260319-g3v-migrate-to-native-shadcn-components-with/260319-g3v-SUMMARY.md`
</output>
